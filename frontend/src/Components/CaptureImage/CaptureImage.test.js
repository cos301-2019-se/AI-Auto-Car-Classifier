import CaptureImage from './CaptureImage';
import { shallow } from 'enzyme';
import React from 'react';

describe('CaptureImage', () => {
    it('Should render', () => {
        const wrapper = shallow(<CaptureImage/>);
        expect(wrapper).not.toBeNull();
    });
    it('Should render', () => {
        const wrapper = shallow(<CaptureImage/>);
        expect(wrapper).not.toBeNull();
    });
    it('Should render', () => {
        const wrapper = shallow(<CaptureImage/>);
        expect(wrapper).not.toBeNull();
    });
    it('Should render', () => {
        const wrapper = shallow(<CaptureImage/>);
        expect(wrapper).not.toBeNull();
    });
    it('Should render', () => {
        const wrapper = shallow(<CaptureImage/>);
        expect(wrapper).not.toBeNull();
    });
    it('Should render', () => {
        const wrapper = shallow(<CaptureImage/>);
        expect(wrapper).not.toBeNull();
    });
});