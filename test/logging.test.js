const res = require('../api/logging');

var assert = require('assert');


  describe('res', function() {
    it('should return 200', function() {
      assert.equal(res.status, res.status);
    });
  });